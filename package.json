{
  "name": "pomodoro",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "wizard": "workbox wizard",
    "sw": "workbox generateSW workbox-config.cjs",
    "lint": "eslint --fix .",
    "push": "pnpm lint && pnpm build && pnpm sw && gh-pages -d dist"
  },
  "devDependencies": {
    "eslint": "8.57.0",
    "gh-pages": "6.1.1",
    "vite": "5.1.5",
    "workbox-cli": "7.0.0"
  }
}
